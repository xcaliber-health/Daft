{{- if and .Values.auth.value (not .Values.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "daft-serve.secretName" . }}
  labels:
    {{- include "daft-serve.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.auth.value | quote }}
{{- end }}
