apiVersion: v1
kind: Service
metadata:
  name: {{ include "daft-serve.fullname" . }}
  labels:
    {{- include "daft-serve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "daft-serve.selectorLabels" . | nindent 4 }}
